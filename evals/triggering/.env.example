# Copy this file to `.env` (gitignored) and set your key.
# GEPA's reflection step proposes description rewrites with an OpenAI model via
# LiteLLM, which reads OPENAI_API_KEY. The task side (running candidate
# descriptions) stays on your authenticated `claude -p`, so only reflection
# needs this key.
OPENAI_API_KEY=sk-...
