/** Placeholder module for Scala 3 skill examples.
  *
  * Add catalog or use-case snippets as files under `src/main/scala/`, then
  * verify with `sbt compile` (and `sbt test` for snippets with munit tests).
  *
  * For automated per-snippet verification of the markdown docs, use the
  * verify-markdown-snippets skill instead — it compiles each fenced block in
  * isolation against this project's Scala version and dependencies.
  */
object Placeholder
